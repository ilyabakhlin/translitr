import {Code} from "../Types/Code";

export namespace Codes
{
    /**
     * @type Code.Invalid[]
     */
    export const Invalid: Code.Invalid[] = [
        [""],
    ];
    /**
     * @type Code.Valid
     */
    export const Valid: Code.Valid[] = [
        ["KeyA", {code: "KeyA"}],
    ];
}
